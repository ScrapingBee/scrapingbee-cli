# YouTube search output

**`scrapingbee youtube-search`** returns JSON: `results` (nested: title, link, channel, etc.).

Batch: output is `N.json` in batch folder. See [reference/batch/output.md](reference/batch/output.md).
